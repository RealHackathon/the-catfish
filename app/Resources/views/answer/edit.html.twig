{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col s12 offset-m1 m10">
            <h1>Edition d'une réponse</h1>

            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <input type="submit" value="Editer" />
            {{ form_end(edit_form) }}

                <p>
                    <a href="{{ path('admin_answer_index', {'id': answer.question.id}) }}"><i class="material-icons left">question_answer</i>Retour aux réponses</a>
                </p>
                <p>
                    {{ form_start(delete_form) }}
                    <input type="submit" value="Supprimer">
                    {{ form_end(delete_form) }}
                </p>
        </div>
    </div>

{% endblock %}
