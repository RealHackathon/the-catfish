{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col s12 offset-m1 m10">
            <h1>Création d'une réponse</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Créer"/>
            {{ form_end(form) }}
            <p>
                <a href="{{ path('admin_answer_index', { 'id': question.id }) }}"><i class="material-icons left">question_answer</i>Retour
                    aux réponses</a>
            </p>
        </div>
    </div>

{% endblock %}
