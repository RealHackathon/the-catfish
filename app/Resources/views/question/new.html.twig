{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col s12 offset-m1 m10">
            <h1>Création d'une question</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Créer" />
            {{ form_end(form) }}

                <p>
                    <a href="{{ path('admin_question_index', { 'id': lesson.id}) }}"><i class="material-icons left">live_help</i>Retour aux questions</a>
                </p>
        </div>
    </div>

{% endblock %}
