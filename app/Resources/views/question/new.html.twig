{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col s12 offset-m1 m10">
            <h1>Création d'une question</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Créer" />
            {{ form_end(form) }}

            <ul>
                <li>
                    <a href="{{ path('admin_question_index', { 'id': lesson.id}) }}">Retour aux questions</a>
                </li>
            </ul>
        </div>
    </div>

{% endblock %}
